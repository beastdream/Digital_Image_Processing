augmentation:
  enabled: false
brightness:
  enabled: false
contrast:
  enabled: false
denoise:
  enabled: true
  gaussian_kernel:
  - 3
  - 3
  gaussian_sigma: 0.8
  method: gaussian
description: Evaluates Gaussian Blur filter to smooth road surface noise.
experiment_name: B_gaussian
normalization:
  enabled: true
  mode: pixel_scale
resize:
  enabled: true
  mode: letterbox
  target_size:
  - 640
  - 640
